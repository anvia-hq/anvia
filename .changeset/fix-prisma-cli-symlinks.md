---
"@anvia/memory-prisma": patch
---

Fix the memory schema CLI silently exiting when invoked through package-manager symlinks. Support Prisma 7 and Prisma 8 initialization from paths containing spaces or URL-special characters.
