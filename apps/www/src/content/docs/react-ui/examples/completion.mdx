---
title: Completion panel
description: Build a prompt-to-text completion panel with @anvia/react-ui.
section: react-ui
sidebar:
  group: Examples
  order: 6
  label: Completion panel
---

import CodeVariants from "../../../../components/docs/CodeVariants.astro";
import { releaseNoteVariants } from "../../../../lib/react-ui-code-variants";

Use completion primitives when the interface is one prompt and one generated text result rather
than a multi-message chat thread.

<CodeVariants id="completion-release-note" variants={releaseNoteVariants} />

The completion endpoint should accept the same `UIStreamRequest` shape as chat and return Anvia
stream events. See [Completion](/docs/react-ui/completion) for the route example.
