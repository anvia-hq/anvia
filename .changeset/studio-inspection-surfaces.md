---
"@anvia/studio": minor
---

Add Studio inspection surfaces for memory, runtime status, richer agent metadata, direct tool invocation, and pipeline replay controls, with in-memory storage as the default and optional SQLite persistence.
