---
"@anvia/studio": patch
---

Keep playground composer selectors compact with single-line model names and control values, preserving full labels in dropdown menus and allowing toolbar controls to wrap on narrow screens.
